# Tmux Aliases
alias t='tmux'
alias ta='tmux attach-session -t'  # Присоединиться к сессии
alias tn='tmux new-session -s'  # Создать новую сессию с именем
alias tl='tmux list-sessions'  # Список активных сессий
alias tk='tmux kill-session -t'  # Удалить сессию
alias ts='tmux send-keys -t'


